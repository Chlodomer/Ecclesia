# Copy to .env (do not commit secrets)

# Feature flags
# Toggle the new in-game tutorial overlay (default: disabled to avoid regressions)
VITE_FEATURE_TUTORIAL=false

# Base path for GitHub Pages (Actions sets this automatically at build time)
# Example for project pages: /Ecclesia/
# BASE_PATH=/
